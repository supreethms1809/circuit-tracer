"""Rebuttal results-extraction scripts (read-only over checkpoints).

New evaluation code for the NeurIPS author response lives here, deliberately
separate from the training and paper-runner packages (see
paper_rebuttal_todo.md §0.3). Nothing in this package mutates checkpoints,
training code, or suite outputs.
"""
