{
  "defaults": [
    "../base/common_gpt2_small.json",
    "../macag/default.json",
    "../models/gpt2_small_linear_feature_match.json",
    "../benchmarks/neurips_core.json"
  ],
  "suite_name": "probe_threshold_fix_gpt2_small",
  "description": "SHORT PROBE (not a paper run). Validates the JumpReLU threshold fix: log-theta split into a no-decay/small-eps AdamW group, jumprelu_bandwidth 2 -> 1e-3, threshold_init 1e-3 -> 1e-2. Success criterion: linear L0/token falls off its ~18k plateau and stats/threshold_mean moves materially off threshold_init. 3000 steps, ~60M tokens, cold start (no resume), train only. Writes to results/paper/probe_threshold_fix_gpt2_small/ and touches no paper_v2/v3 results.",
  "seeds": [101],
  "stages": {
    "collect_dataset": true,
    "train": true,
    "evaluate": false,
    "macag": false,
    "report": false
  },
  "dataset": {
    "n_tokens": 60000000,
    "collection_chunk_n_tokens": 20000000,
    "max_train_samples": 445312,
    "cache_dir": "/lscratch/probe_threshold_fix/gpt2_small/activations/train",
    "val_cache_dir": "/gscratch/ssuresh/shared/activations/probe_threshold_fix/gpt2_small/val",
    "eval_samples": 500
  },
  "model_variants": {
    "linear_feature_match_gpt2_small_pv3": {
      "training": {
        "use_fsdp": true,
        "resume_training_if_exists": false,
        "fsdp_cpu_offload": false,
        "total_steps": 3000,
        "warmup_steps": 300,
        "log_every": 20,
        "eval_every": 500,
        "save_every": 1000,
        "keep_last_checkpoints": 1,
        "run_name": "probe_linear_fm_threshold_fix"
      }
    }
  },
  "reporting": {
    "primary_variant": "linear_feature_match_gpt2_small_pv3",
    "baseline_variant": "linear_feature_match_gpt2_small_pv3",
    "bootstrap_samples": 100,
    "confidence_level": 0.95,
    "figure_case_study_prompt_id": "ioi_john_mary"
  }
}
